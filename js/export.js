/**
 * Mudbrick — Export (Phase 5)
 * Hybrid export: pdf-lib drawing primitives for shapes/text,
 * raster fallback for freehand paths.
 * Stub — will be implemented in Phase 5.
 */

export async function exportAnnotatedPDF(state) {
  // Phase 5: Bake annotations via hybrid pipeline
  return null;
}

export async function exportFlattenedPDF(state) {
  // Phase 5: Flatten pages with Visual Cover to raster
  return null;
}

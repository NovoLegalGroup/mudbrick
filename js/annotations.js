/**
 * Mudbrick — Annotations (Phase 4)
 * Fabric.js overlay: draw, highlight, text, shapes, stamps, visual cover.
 * Stub — will be implemented in Phase 4.
 */

export function initAnnotations() {
  // Phase 4: Initialize Fabric.js overlay canvas
}

export function setTool(toolName) {
  // Phase 4: Switch active annotation tool
}

export function savePageAnnotations(pageNum) {
  // Phase 4: Serialize current Fabric canvas to JSON
  return null;
}

export function loadPageAnnotations(pageNum, json) {
  // Phase 4: Restore Fabric canvas from JSON
}

export function resizeOverlay(width, height) {
  // Phase 4: Resize Fabric canvas to match PDF viewport
}

export function dispose() {
  // Phase 4: Clean up Fabric canvas instance
}

/**
 * Mudbrick — Forms (Phase 6)
 * PDF form field detection and filling via Fabric.js + pdf-lib writeback.
 * Stub — will be implemented in Phase 6.
 */

export function detectFormFields(pdfLibDoc, pageNum) {
  // Phase 6: Read AcroForm fields and return positions
  return [];
}

export function renderFormFields(fields, fabricCanvas) {
  // Phase 6: Create Fabric objects for form fields
}

export function writeFormValues(pdfLibDoc, fieldValues) {
  // Phase 6: Write filled values back to pdf-lib form
}
